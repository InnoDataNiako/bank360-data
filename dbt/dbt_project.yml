name: bank360
version: 1.0.0
config-version: 2

profile: bank360

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
seed-paths: ["seeds"]

target-path: "target"

clean-targets:
  - "target"
  - "dbt_packages"

vars:
  bronze_schema: bronze
  silver_schema: silver
  gold_schema: gold

models:
  bank360:
    gold:
      +materialized: table
      +file_format: iceberg